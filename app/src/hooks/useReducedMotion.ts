// InfiniDrive — Re-export Framer Motion's reduced-motion hook so the
// whole app can honor OS "reduce motion" preferences consistently.
// Use in animated components to swap springy transitions for instant ones.
export { useReducedMotion } from 'framer-motion';
